**易维保智能管理App课程大作业报告**

**小组成员：__柴伯涛_______**

**小组成员：__徐帅_________**

**小组成员：__徐佳豪_______**

**小组成员：__张佳硕_______**

完成日期：2026 年 07 月 24 日

**项目基本信息**

1.项目名称：易维保智能管理 App

2.项目定位：面向中小制造企业的移动端设备维保管理平台，以 PC 端工业维保系统 4.0 为蓝本，将设备台账、工单维保、故障分析等业务迁移至 Android 移动端，实现工厂设备全流程移动化运维管理。

3.整体技术栈：

- 后端：Spring Boot 3.3.5 + Java 21 + MySQL 9.3
- 移动端：Kotlin 2.0 + Jetpack Compose + Material 3

**二、项目功能介绍**

本项目 Demo 阶段功能分为核心基础功能、扩展增值功能两层，全部开发完成。

**（一）核心层功能**

- **设备台账管理**
支持设备新增、编辑、查询、报废、删除；提供分页搜索、设备二维码生成与扫码识别；删除设备前校验，存在未完成工单时禁止删除。
- **维修工单闭环管理**
完整业务流程：报修→维修工接单→填写维修记录→工单完成 / 撤销，工单内置独立状态机，设备状态与工单状态自动联动变更。
- **故障数据统计**
包含故障整体概览、故障分类统计、维修平均耗时计算、设备故障 Top 排行图表展示。
- **用户权限认证**
基于 JWT 实现账号登录、注册；区分操作工、维修工、管理员三类角色，配套接口访问权限控制，Token 本地持久化保存。

**（二）扩展层功能**

- **设备运行监控**
内置模拟传感器数据生成引擎，自动仿真设备振动、温度、电流、功率、压力、转速实时运行数据，无需接入真实物联网硬件。
- **设备健康评分**
搭建振动、温度、电气三维加权评分模型，实时计算设备健康分数，划分健康、关注、告警三级状态。
- **智能故障诊断**
内置 47 条故障诊断规则库，多条件匹配引擎自动识别设备异常；维修工单完成后自动沉淀维修案例，案例可审核并升级为新诊断规则，实现知识库自迭代。
- **维修知识库**
所有维修案例统一归档，支持全文关键词检索，历史维修经验可复用查阅。
- **预测性维护**
设备退化仿真引擎模拟 12~48 小时设备损耗周期，基于历史传感数据线性回归计算 RUL 设备剩余使用寿命，提前预警故障。

**三、系统整体架构**

项目采用前后端分离架构，Android 客户端通过 HTTP JSON 接口与 Spring Boot 后端交互。

**1. Android 客户端分层架构（MVVM）**

- UI 层：Jetpack Compose 声明式页面，单 Activity 导航架构，Navigation Compose 统一路由管理；集成 CameraX+ZXing 实现扫码功能。
- ViewModel 层：基于 StateFlow 管理页面状态，协程处理异步业务，隔离界面与数据逻辑。
- Repository 层：统一封装网络请求，捕获接口异常，作为 UI 与网络层中间桥梁。
- 网络底层：Retrofit+OkHttp 实现网络请求，Token 拦截器自动携带登录凭证；DataStore 持久化存储用户登录信息。

**2. Spring Boot 后端分层架构**

标准四层结构：Controller 接口层 → Service 业务层 → Repository 数据访问层 → Entity 数据库实体层。

- Controller：共 8 个接口控制器，分别处理认证、设备、工单、统计、健康监控、智能诊断、传感器数据、文件上传业务。
- Service：6 个核心业务服务 + 2 个仿真引擎（传感器数据生成器、设备退化引擎），承载全部业务逻辑、事务控制、状态流转、算法计算。
- 安全模块：JWT 无状态认证、BCrypt 密码加密、跨域配置、全局统一异常捕获。
- 数据存储：MySQL 9.3 持久化业务数据，本地文件夹存储设备二维码、工单照片等文件。

**架构设计特点**

- 后端采用单体架构，适合课程 Demo 快速开发、调试；后续可拆分为微服务架构。
- 移动端无本地数据库离线缓存，全部数据实时请求后端接口。
- 全系统无真实 IoT 硬件依赖，所有设备损耗、传感数据由程序内置引擎仿真生成。
- 认证采用无状态 JWT，移动端适配多设备登录场景。

**四、后端详细设计**

**（一）技术选型说明**

组件

技术选型

选用理由

开发框架

Spring Boot 3.3.5

企业级成熟框架，自动配置，生态完善

开发语言

Java 21

LTS 长期支持版本，支持虚拟线程、模式匹配新特性

ORM 框架

Spring Data JPA+Hibernate

自动生成数据表，简化 CRUD 代码开发

数据库

MySQL 9.3

稳定开源关系型数据库，适配中小型业务数据存储

安全认证

Spring Security + JJWT 0.12.6

实现无状态登录鉴权，接口权限拦截

密码加密

BCrypt

单向哈希加密，防止密码泄露、彩虹表破解

二维码工具

ZXing 3.5.3

Java 跨平台生成、解析设备二维码

**（二）核心业务模块设计**

- **设备管理模块**
提供设备全生命周期接口，创建设备时自动生成专属设备二维码；设备绑定独立状态（正常 / 待维修 / 维修中 / 报废），工单操作会同步变更设备运行状态；报废设备无法发起报修工单。
- **维修工单模块**
内置工单状态流转规则：待处理→处理中→已完成，待处理工单支持撤销；创建工单自动标记设备故障，维修完成后设备恢复正常状态；工单完成自动生成维修诊断案例归档。
- **设备健康评分模块**
三维加权计算公式：总分 = 振动分 ×40% + 温度分 ×30% + 电气分 ×30%

- 振动分依据 ISO 10816 工业标准，根据设备额定功率设置基准阈值计算；
- 温度分基于设备出厂最高耐受温度计算扣分；
- 电气分综合电流、功率偏移程度计算；
系统根据总分划分健康等级，同时基于 30 分钟历史数据线性回归，估算设备剩余使用寿命 RUL。

- **智能诊断规则引擎**
内置 47 条预设故障规则，每条规则包含触发条件、故障原因、维修方案；支持多条件与或逻辑匹配设备实时传感数据，自动推送 Top5 故障诊断建议。
工单完成后自动生成维修案例，案例经审核后可自动提取故障条件，升级为全新诊断规则，持续扩充故障知识库。
- **仿真数据引擎**
定时任务每 10 秒生成一次设备传感数据，根据早中晚不同时段设置生产负载系数；退化引擎持续累加轴承、刀具、冷却系统损耗，损耗程度同步影响振动、温度、电流等传感器数值；系统每日凌晨自动清理 7 天前历史传感数据，避免数据库冗余。

**五、数据库设计**

数据库采用 MySQL，共 7 张核心业务数据表，表间通过外键关联，整体 ER 关系如下：

- users 用户表：存储操作工、维修工、管理员账号、角色、加密密码；
- equipment 设备表：保存所有生产设备基础信息、额定参数、运行状态、二维码存储路径；
- work_orders 工单表：记录报修工单全量信息，关联对应设备，存储故障描述、维修方案、工单状态；
- machine_data 传感数据表：定时存储每台设备振动、温度、电流、转速等实时运行数据；
- equipment_health 设备健康表：记录轴承、刀具、冷却三大维度磨损损耗数值；
- diagnosis_rules 诊断规则表：存储全部故障判断规则、维修建议、适用机型；
- diagnosis_cases 维修案例表：工单完成自动生成，存储历史维修记录、设备故障传感快照，用于检索与规则升级。

各表使用 bigint 自增主键，文本类长数据采用 TEXT 存储，设备编码、工单编号设置唯一约束，保证数据不重复。



**六、接口 API 设计规范**

- 统一返回格式
所有接口标准化 JSON 返回，code 区分成功 / 客户端错误 / 服务端错误，message 返回提示文字，data 承载业务数据。
- 分页接口统一封装
列表查询接口统一返回分页对象，包含当前页数据、总条数、总页数、页码、每页条数。
- 全局异常处理
通过统一异常处理器捕获参数校验异常、业务运行异常、系统未知异常，分别返回对应错误码，不对外暴露服务器底层报错堆栈。
- 接口分类

- 公开接口：登录、注册、静态文件 / 二维码图片访问，无需携带 Token；
- 认证接口：设备、工单、统计、监控、诊断等全部业务接口，请求头携带 Bearer Token 鉴权。

**七、安全机制设计**

**1. 用户登录认证流程**

用户提交账号密码，后端使用 BCrypt 校验密码，校验通过生成 24 小时有效期 JWT 令牌，返回移动端；后续所有业务请求拦截器自动解析 Token，校验签名与有效期，完成身份认证后执行业务逻辑。

**2. 安全防护措施**

- 密码全部 BCrypt 单向加密存储，数据库无明文密码；
- JWT 令牌使用 HMAC-SHA256 加密签名，防止篡改；
- REST 接口关闭 CSRF 校验，采用无状态会话；
- 配置跨域访问规则，区分公开接口与受保护业务接口；
- 服务端异常屏蔽底层报错信息，防止敏感信息泄露。

**3. Demo 阶段安全简化说明**

本项目为课程演示版本，存在多处生产环境优化空间：

- JWT 密钥、数据库账号密码写在配置文件，生产需改用环境变量 / 密钥管理工具；
- 跨域允许全部前端域名访问，生产环境需配置域名白名单；
- 未配置 HTTPS、接口访问限流、登录令牌刷新机制；
- 文件存储仅使用本地服务器文件夹，正式项目可替换对象存储 OSS。

**八、Android 客户端设计**

**1. 开发技术选型**

- 开发语言：Kotlin 2.0
- UI 框架：Jetpack Compose + Material3
- 网络请求：Retrofit2 + OkHttp
- 本地存储：DataStore（存储登录 Token、用户信息）
- 扫码模块：CameraX 相机库 + ZXing 解析二维码
- 最低适配版本：Android 8.0（API26）

**2. 页面与导航结构**

采用单 Activity 全局抽屉导航，共 17 个功能页面：登录页、首页、设备管理全套页面、工单全套页面、扫码页、故障统计、设备实时监控、诊断知识库；支持设备二维码 Deep Link 跳转，扫码后直接打开对应设备详情页面。



**3. 核心业务流程（工单维修闭环）**

- 操作工移动端发起报修：扫码选择设备，填写故障描述、上传现场照片，提交生成待处理工单，设备同步标记为待维修；
- 维修工查看待处理工单，点击接单，工单变更为维修中，设备状态同步更新；
- 维修完成后填写故障诊断、维修措施、更换备件，提交完成工单；
- 后端自动归档维修案例，重置设备磨损数据，设备恢复正常运行状态；
- 仅待处理工单支持撤销，撤销后若无其他维修工单，设备恢复正常状态。

**九、项目技术取舍与扩展方案**

**（一）开发阶段关键技术决策**

- 后端选用单体 Spring Boot：课程 Demo 开发效率高，降低部署调试难度，正式项目可拆分微服务；
- 数据层使用 JPA 自动建表：减少建表 SQL 编写，开发调试便捷，生产环境改用数据库迁移脚本；
- 不接入真实 IoT 设备：内置仿真模拟传感数据，脱离硬件环境即可完整演示全部监控、预测维护功能；
- 故障诊断采用规则引擎，不依赖大模型 API：项目独立运行，无第三方接口调用成本，规则可后台动态新增。

**（二）系统可扩展设计点**

- 设备数据表预留全套物联网传感额定参数，后期可直接对接真实传感器采集数据；
- 诊断规则使用 JSON 条件表达式，运行时动态新增、修改规则，无需改动后端代码；
- 磨损退化引擎基于物理损耗模型设计，可拓展更多设备故障损耗算法；
- API 预留版本化拓展能力。